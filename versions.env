# Central version pins — edit here, not in individual files.
# Read by GitHub workflows; Dockerfiles receive values as build-args.
# devcontainer.json carries its own fallback — keep it in sync.
UV_VERSION=0.11.1
PYTHON_VERSION=3.12
